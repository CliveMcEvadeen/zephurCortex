[tool.poetry]
name = "zephyrcotex"
version = "0.1.0"
description = "initial testing"
authors = ["clive <clivekakeeto48@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
